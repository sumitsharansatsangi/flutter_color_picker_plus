name: flutter_color_picker_example
description: A flutter_color_picker example Flutter project.
version: 1.0.0+1

publish_to: none

dependencies:
  flutter:
    sdk: flutter
  flutter_color_picker:
    path: ../

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

environment:
  sdk: '>=3.1.0 <4.0.0'
